#include<stdio.h>
int main(){
    int l,w,sum;
    printf("Enter a Length => ",l);
    scanf("%d",&l);

    printf("Enter a Width => ",w);
    scanf("%d",&w);

    sum=l*w;

    printf("Area Of A Rectangle => %d",sum);
}