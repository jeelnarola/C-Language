#include<stdio.h>
#define pi 3.14
int main()
{
    int a,sum;
    printf("enater a A number =>",a);
    scanf("%d",&a);

    sum=2*pi*a;  // =pi*r*r => 3.14*a*a
    
    printf("area of a circle ANS => %d",sum);


}