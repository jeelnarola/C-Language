
// 1 Write a Program to find the area of a circle.

#include<stdio.h>
#define pi 3.14
int main()
{
    int a,area;
    printf("enater a A number =>",a);
    scanf("%d",&a);

    area=pi*a*a;  // =pi*r*r => 3.14*a*a
    
    printf("area of a circle ANS => %d",area);


}